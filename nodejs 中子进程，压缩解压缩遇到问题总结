导入导出模块是云配置系统数据核心功能
架构为一个应用进程根据cpu数fork子进程进行功能操作
问题
1，有天用户发现导入导出功能不能用了
查log,linux 进程发现fork子进程都已被杀死，但未重启，于是修改代码在收到fork子进程死自后在原来子进程数组中更换被杀子进程
2，单个导入功能阻塞
查log,发现导入导出功能代码中由于使用async组件，而部分地方报错而未callback导致。此处说明，导入解压缩后文件分很多子文件分异步转换，待全部转换完成callback主入口
3,导入导出CRT9200data 大包导致内存不足问题
原使用local-zip 未使用stream方式，导致内存不够才会这样，这次改用unzipper解压和 archiver压缩，这其中还发现unzipper组件不稳定，有时候会收不到解压完成事件close。目前这个组件也一直在持续更新版本中，应该也是知道有这方面的问题。
4，导入导出子进程阻塞
导入导出子进程工作时，在应用进程数组内标记work，待完成后恢复idle。由于使用nodejs WriteStream close事件监听不会每次都返回导致子进程不能恢复idle，这也是3中close不靠谱原因之一
